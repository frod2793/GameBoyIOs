using UnityEngine;

namespace DogGuns_Games.vamsir
{
    public class Bamser_Mob_Base : MonoBehaviour
    {
        // Start is called once before the first execution of Update after the MonoBehaviour is created
        void Start()
        {
        }

        // Update is called once per frame
        void Update()
        {
        }
    }
}